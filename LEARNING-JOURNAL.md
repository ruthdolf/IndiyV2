Here is what I have learned about the web app architecture (generated by Google AI Studio). Some explanations are COMMENTS in the relevant files, some are in Figma mindmap :D

**23 April**
Relevant files:
1. index.html
2. main.tsx
3. App.tsx
4. Marketplace.tsx
5. firebase.ts
6. ServiceCard.tsx
7. types.ts

What I've learned:
-How is main.tsx a bridge between index.html and App.tsx?

In App.tsx, 
-understand the hierachy and use of ErrorBoundary, AuthProvider, Router, Layout and Routes
-What does <Routes> do?

In Marketplace.tsx,
-What is a functional component, how to define it (syntax), what does it return?
-What is a state? 
-What is a hook? How does useEffect() work?
-How do a functional component, state and hook work together?
-What does const [services, setServices] = useState<Service[]>([]); mean?
-What is essentially in types.ts, ServiceCard.tsx, and firebase.ts?
-How and why is Marketplace.tsx "connected" to types.ts, ServiceCard.tsx, and firebase.ts?
-What does Marketplace component return?

In ServiceCard.tsx
-What does interface mean?
-What is a type argument? Why does ServiceCard component have it, but Marketplace doesn't?

-In Firebase, there're 2 Firestore databases (71f3 and the other). 71f3 is currently in use in firebase-applet-config.json. (Idk what the other one is about yet)




**24 April**
Relevant Files:
1. Marketplace.tsx
2. firebase.ts

What I've learned:
-In firebase.ts, what do 'async' and 'await' mean? What would happen if we delete await?

In Marketplace.tsx
-What does const [loading, setLoading] = useState(true); mean?
-What is "finally" block in try, catch, finally statements in useEffect()?
-How does JSX switch from Spinner to Services? ie. Component lifecycle 
-Why do we need .map() after we fetch data from Forebase?
-How does .filter() work?
-How does .includes() work?
-How does the code in useEffect() show services of all categories?
-How does the code in useEffect() show services when there's a search query?
-What is the difference between filtering for category and filtering for searchQuery? (logic, location and why)

**26 April**
ServiceCard.tsx

What I've learned:
-Why does ServiceCard need UserProfile info from AuthContext?
-The difference between type (interface AuthContextType in AuthContext.tsx) and props (interface ServiceCardProps in ServiceCard.tsx). (And again what does interface essentially mean?)

